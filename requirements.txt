# Author: sizhchan | Org: dgaudit | Version: v0.1 | Date: 2026-05-27
# ── PDF 预处理与图像清洗 ──
PyMuPDF==1.24.14             # 被 magic-pdf 限定 <1.25.0
opencv-contrib-python==4.10.0.84  # PaddleOCR 精确锁定
pypdfium2==5.8.0             # PaddleOCR 自带，高性能 PDF 渲染

# ── OCR 引擎 (PaddleOCR 3.x = PaddleX 架构) ──
paddleocr==3.6.0
paddlepaddle==3.0.0          # PaddleX 运行时推理引擎 (3.3.1 有 ONEDNN bug)

# ── 文档大模型解析 (MinerU) ──
magic-pdf==1.3.12

# ── YOLOv8 目标检测 ──
ultralytics==8.4.53

# ── 多模态大模型推理 (CPU 版 PyTorch 在 Dockerfile 中单独安装) ──
# torch + torchvision 通过 --index-url https://download.pytorch.org/whl/cpu 安装

# ── 数据清洗与导出 ──
pandas==3.0.3
openpyxl==3.1.5
python-docx==1.2.0

# ── 数据校验 ──
pydantic==2.10.6             # magic-pdf 要求 <2.11，PaddleOCR 要求 >=2

# ── 版本冲突调和 ──
numpy==1.26.4                 # PaddleOCR 要求 <2.4，ultralytics/magic-pdf 兼容

# ── Web 框架 ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
jinja2==3.1.4

# ── 认证 ──
passlib[bcrypt==4.0.1]==1.7.4
python-jose[cryptography]==3.3.0

# ── 数据库 ──
aiosqlite==0.20.0

# API / Auth
aiofiles
